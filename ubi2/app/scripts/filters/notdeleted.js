'use strict';

/**
 * @ngdoc filter
 * @name ubiApp.filter:notDeleted
 * @function
 * @description
 * # notDeleted
 * Filter in the ubiApp.
 */
angular.module('ubiApp')
  .filter('notDeleted', function () {
    return function (input) {
        
    };
  });
